# Phase 1 — non-secret placeholders only. Copy to .env and adjust.
# NEVER put API keys, secrets, or exchange credentials in this file or in .env for Phase 1.

MODE=PAPER
LIVE_TRADING=FALSE
DATABASE_URL=sqlite:///./data/lab.db
LOG_LEVEL=INFO
SYMBOLS=BTCUSDT,ETHUSDT
